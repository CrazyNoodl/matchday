// Storybook-only stub — see expo-media-library-legacy.ts for why.
export async function isAvailableAsync() {
  return false;
}

export async function shareAsync(_uri: string, _options?: unknown) {
  return null;
}
